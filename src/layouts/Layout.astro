<style is:global>
  body {
    display: flex;
    flex-direction: column;
    min-height: 100vh; /* 画面全体の高さを確保 */
  }
  main {
    flex: 1; /* コンテンツが少ない時もフッターを下に押しやる */
  }
  /* ヘッダーが sticky の場合、コンテンツの開始位置を調整 */
  .global-container {
    padding-top: 40px; 
  }
</style>